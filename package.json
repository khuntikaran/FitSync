{
  "name": "fitsync",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ci": "npm run typecheck && vitest run --coverage"
    "test:coverage": "vitest run --coverage"
    "test:watch": "vitest"
  },
  "dependencies": {
    "zustand": "^4.4.6"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "typescript": "^5.2.2",
    "vitest": "^4.0.18"
  }
}
